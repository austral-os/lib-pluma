# Class `pluma::diagnostics::ScopedTimer`

**RAII timer for automatic performance tracking of code blocks.**

## Public Methods
- `ScopedTimer(const std::string &event_name)` - *Starts the timer for the given event name.*
- `~ScopedTimer()` - *Stops the timer and records the result to the*

