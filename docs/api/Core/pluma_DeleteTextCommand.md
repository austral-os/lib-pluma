# Class `pluma::DeleteTextCommand`

**A command that removes text from the document.**

## Public Methods
- `DeleteTextCommand(uint32_t offset, uint32_t length)` - *Constructs a*
- `void execute(PieceTable &table) override` - *Executes the command against a piece table.*

