# Class `pluma::plugins::MarkdownImporter`

**Basic parser that converts markdown into Pluma text structure.**

## Public Methods
- `bool import(const std::string &data, PlumaEditor &editor) override` - *Parses an external format and loads it into the editor.*

