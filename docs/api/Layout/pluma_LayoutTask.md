# Struct `pluma::LayoutTask`

**A unit of work for the**

## Public Methods
- `bool operator<(const LayoutTask &other) const` - *Comparator for priority queues (lower enum value = higher priority).*

