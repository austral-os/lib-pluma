# Class `pluma::InsertTextCommand`

**A command that inserts text into the document.**

## Public Methods
- `InsertTextCommand(uint32_t offset, std::string text)` - *Constructs an*
- `void execute(PieceTable &table) override` - *Executes the command against a piece table.*

