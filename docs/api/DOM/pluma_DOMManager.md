# Class `pluma::DOMManager`

**Handles creation, synchronization, and updates of the DOM tree based on the**

## Public Methods
- `std::unique_ptr< DocumentNode > rebuild(const PieceTable &pieceTable)` - *Rebuilds the entire logical DOM tree by reading the*

