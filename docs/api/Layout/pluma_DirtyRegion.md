# Struct `pluma::DirtyRegion`

**Represents a continuous span of text offsets that have been invalidated.**

## Public Methods
- `bool overlaps(const DirtyRegion &other) const` - *Checks if this region overlaps with another.*

